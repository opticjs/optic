'use strict';

function all(query) {
  return query;
}

function start(query, start) {
  return query;
}

function count(query, count) {
  return query;
}

function for(query, parent) {
  return query;
}

function params(query, params) {
  return query;
}

function data(query, params) {
  return query;
}

module.exports = {
  all: all
};
